#include <iostream>

using namespace std;

#include "IntList.h"
#include "SortedSet.h"

int main() {

    // SortedSet sset5;
    // cout << "sset5 has been constructedL tests add" << endl;
    // sset5.add(0);
    // sset5.add(700);
    // sset5.add(30);
    // sset5.add(1);
    // sset5.add(16);
    // sset5.add(156);
    // cout << "Set 5 : " << sset5 << endl;

    // SortedSet sset6;
    // cout << "sset6 has been constructed: tests push_front, push_back, and insert_ordered" << endl;
    // sset6.push_front(100);
    // sset6.push_front(19);
    // sset6.push_front(28);
    // sset6.push_back(83);
    // sset6.push_back(689);
    // sset6.push_back(215);
    // sset6.insert_ordered(73);
    // sset6.insert_ordered(3);
    // sset6.insert_ordered(300);
    // cout << "Set 6 : " << sset6 << endl;

    // cout << "testing in using sset5 and sset6" << endl;
    // cout << "0 should return IN for sset5: " << (sset5.in(0) ? "in" : "not in") << endl;
    // cout << "156 should return IN for sset5: " << (sset5.in(156) ? "in" : "not in") << endl;
    // cout << "30 should return IN for sset5: " << (sset5.in(30) ? "in" : "not in") << endl;
    // cout << "15 should return NOT IN for sset5: " << (sset5.in(15) ? "in" : "not in") << endl;
    // cout << "1000 should return NOT IN for sset5: " << (sset5.in(1000) ? "in" : "not in") << endl;
    // cout << endl;
    // cout << "19 should return IN for sset6: " << (sset6.in(19) ? "in" : "not in") << endl;
    // cout << "689 should return IN for sset6: " << (sset6.in(689) ? "in" : "not in") << endl;
    // cout << "73 should return IN for sset6: " << (sset6.in(73) ? "in" : "not in") << endl;
    // cout << "0 should return NOT IN for sset6: " << (sset6.in(0) ? "in" : "not in") << endl;
    // cout << "500 should return NOT IN for sset6: " << (sset6.in(500) ? "in" : "not in") << endl;

    // SortedSet sset7;
    // SortedSet sset8;
    // SortedSet sset9;
    // cout << "sset7 and sset8 has been constructed: tests operators | and |=" << endl;
    // sset7.add(0);
    // sset7.add(1);
    // sset7.add(2);
    // sset7.add(3);
    // sset7.add(4);
    // sset7.add(5);
    // sset8.add(10);
    // sset8.add(11);
    // sset8.add(12);
    // sset8.add(13);
    // sset8.add(14);
    // sset8.add(15);
    // cout << "combining sset7 and sset8" << endl;
    // sset9 = sset7 | sset8;
    // cout << "Set 9 = Set 7 | Set 8 : " << sset9 << endl;

    // SortedSet sset10;
    // SortedSet sset11;
    // SortedSet sset12;
    // cout << "sset10 and sset11 has been constructed: tests operators & and &=" << endl;
    // sset10.add(49);
    // sset10.add(45);
    // sset10.add(80);
    // sset10.add(9);
    // sset10.add(34);
    // sset11.add(80);
    // sset11.add(0);
    // sset11.add(81);
    // sset11.add(60);
    // sset11.add(45);
    // cout << "combining shared numbers" << endl;
    // sset12 = sset9 & sset10;
    // cout << "Set 12 = Set 10 & Set 11 : " << sset 12 << endl;

    return 0;
}